Skills = step-by-step how-tos (prerequisites, numbered steps, verification, failure recovery).

USE WHEN you ran a repeatable procedure (deploy, secrets, MCP/debug playbook, ingest, review, support) — not a one-off patch note.

Pick one: skill_save (how-to) | specs_propose document_type+content (long doc) | specs_propose purpose/invariants (code spec) | save_memory (short truth).

skill_get before procedural work; skill_save before release_work.
